import styled from "styled-components";
import { Input } from "@mui/material";

const InputButton = styled(Input)`
  border-radius: 5px;
  background-color: rgba(100, 180, 20, 0.75);
  height: 3rem;
  width: 100%;
  padding: 15px;
  cursor: pointer;
`;

export default InputButton;
